generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Student {
  id         Int    @id @default(autoincrement())
  name       String
  email      String @unique
  rollNo     String @unique
  department String
  year       Int
  phone      String
  bio        String @default("")
}
